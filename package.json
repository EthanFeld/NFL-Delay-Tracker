{
  "name": "nfl-delay-tracker",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["web"],
  "scripts": {
    "web:dev": "npm run dev --workspace web",
    "web:build": "npm run build --workspace web",
    "web:test": "npm run test --workspace web"
  }
}
